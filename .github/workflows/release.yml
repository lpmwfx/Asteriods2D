name: Release builds

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            love_url: https://github.com/love2d/love/releases/download/11.5/love-11.5-x86_64.AppImage
          - os: macos-latest
            platform: macos
            love_url: https://github.com/love2d/love/releases/download/11.5/love-11.5-macos.zip
          - os: windows-latest
            platform: windows
            love_url: https://github.com/love2d/love/releases/download/11.5/love-11.5-win64.zip

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Derive version and package name
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          else
            SHORT_SHA="$(git rev-parse --short HEAD)"
            VERSION="main-${SHORT_SHA}"
          fi
          APP_NAME="AsteroidDefence2D"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "APP_NAME=$APP_NAME" >> "$GITHUB_ENV"
          echo "LOVE_FILE=dist/${APP_NAME}-${VERSION}.love" >> "$GITHUB_ENV"

      - name: Build .love package
        shell: bash
        run: python .github/scripts/build_love.py

      - name: Package Linux AppImage
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          curl -L "${{ matrix.love_url }}" -o love.AppImage
          chmod +x love.AppImage
          cat love.AppImage "${LOVE_FILE}" > "dist/${APP_NAME}-linux-${VERSION}.AppImage"
          chmod +x "dist/${APP_NAME}-linux-${VERSION}.AppImage"

      - name: Package macOS app
        if: matrix.platform == 'macos'
        shell: bash
        run: |
          set -euo pipefail
          curl -L "${{ matrix.love_url }}" -o love.zip
          unzip -q love.zip
          APP_DIR="$(find . -maxdepth 1 -type d -name '*ove.app' | head -n 1)"
          if [[ -z "${APP_DIR}" ]]; then
            echo "love.app not found after unzip" >&2
            exit 1
          fi
          mv "${APP_DIR}" "${APP_NAME}.app"
          mkdir -p "${APP_NAME}.app/Contents/Resources"
          cp "${LOVE_FILE}" "${APP_NAME}.app/Contents/Resources/game.love"
          if [ -x /usr/libexec/PlistBuddy ]; then
            /usr/libexec/PlistBuddy -c "Set :CFBundleName ${APP_NAME}" "${APP_NAME}.app/Contents/Info.plist" || true
            /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName ${APP_NAME}" "${APP_NAME}.app/Contents/Info.plist" || true
          fi
          ditto -c -k --sequesterRsrc --keepParent "${APP_NAME}.app" "dist/${APP_NAME}-macOS-${VERSION}.zip"

      - name: Package Windows executable
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          Invoke-WebRequest "${{ matrix.love_url }}" -OutFile "love.zip"
          Expand-Archive "love.zip" -DestinationPath "love"
          $zipName = "$env:APP_NAME-windows-$env:VERSION.zip"
          New-Item -ItemType Directory -Path "dist/windows" -Force | Out-Null
          Copy-Item $env:LOVE_FILE -Destination "dist/windows/"
          Copy-Item "love/love-11.5-win64/*" -Destination "dist/windows/" -Recurse
          Push-Location dist/windows
          cmd /c "copy /b love.exe+$env:APP_NAME-$env:VERSION.love $env:APP_NAME.exe"
          Remove-Item love.exe
          Remove-Item "$env:APP_NAME-$env:VERSION.love"
          Compress-Archive -Path * -DestinationPath "..\$zipName"
          Pop-Location

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}
          path: |
            dist/*.zip
            dist/*.AppImage
            dist/*.love
          if-no-files-found: ignore

  publish:
    name: Publish release
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Publish to GitHub Releases
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
